using UnityEngine;

namespace MetaGameUtils
{
    public class MetaUtilsTest : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
            ShowDesktop.ShowDesktopFunc();
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
